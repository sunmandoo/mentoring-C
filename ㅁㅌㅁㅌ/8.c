#include <stdio.h>

int main()
{
    int a;
    int b100;
    int b10;
    int b0;

    printf("변수1의 값: ");
    scanf("%d", &a);
    printf("변수2의 값: ");
    scanf("%d", &a);

    printf("첫 번째 결과값: ");  // 385에서 일의 자리숫자, 십의 자리숫자, 백의 자리숫자를 따로 빼는 방법을 모르겠음.
}